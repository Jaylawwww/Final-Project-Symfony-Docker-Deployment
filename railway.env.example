# Copy these to your Railway APP service → Variables (not the MySQL service).
# Replace "MySQL" with your MySQL service name if you renamed it.

DATABASE_URL=${{MySQL.MYSQL_URL}}
APP_SECRET=replace-with-openssl-rand-hex-32
APP_ENV=prod
APP_DEBUG=0
